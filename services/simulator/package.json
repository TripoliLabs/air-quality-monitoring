{
  "name": "@aq/simulator",
  "version": "0.0.0",
  "private": true,
  "description": "Simulated LoRaWAN deployment — emits realistic ChirpStack uplinks for the fixture nodes",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aq/db": "workspace:*",
    "@chirpstack/chirpstack-api": "^4.0.0",
    "@grpc/grpc-js": "^1.10.0",
    "lora-packet": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
