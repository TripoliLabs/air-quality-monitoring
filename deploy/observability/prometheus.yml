global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Service metrics are pushed in by Alloy via remote-write (see alloy/config.alloy),
# so Prometheus itself scrapes nothing here.
scrape_configs: []
