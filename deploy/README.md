# deploy/ — runtime composition

- `observability/` — LGTM stack configs (Prometheus, Grafana provisioning, Loki/Tempo/Mimir/Alloy).
- `onprem/` — gateway-box provisioning.

Local full-stack dev uses the root `docker-compose.yml`.
