# On-prem deployment

Ansible / cloud-init for gateway boxes (ChirpStack host + optional
store-and-forward agent). Designed for intermittent connectivity (offline
buffering, reconnect/backoff). See docs/architecture.md.
